interface:
  display_name: "CFST Orchestrator"
  short_description: "Prepare rawdata and orchestrate CFST extraction"
  default_prompt: "Use $cfst-orchestrator to preprocess rawdata paper packages, prepare batch manifests, launch isolated per-paper workers with $cfst-column-extractor, track retries/state, and publish validated schema 2.0.0-draft CFST column outputs."
