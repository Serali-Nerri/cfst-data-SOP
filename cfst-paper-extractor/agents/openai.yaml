interface:
  display_name: "CFST Paper Extractor"
  short_description: "Extract and validate v2.3 CFST column JSON"
  default_prompt: "Use $cfst-paper-extractor to extract schema-v2.3 CFST column JSON with shared_context, series_definitions, authoritative ordinary_decisions scratch YAML, explicit `material_modifiers=[]` proof on ordinary rows, specimen-level ordinary tagging, and sandbox validation."
